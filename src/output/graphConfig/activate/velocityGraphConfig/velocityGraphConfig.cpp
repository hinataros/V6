/**
   @author Sho Miyahara 2018
*/

#include "config.hpp"
#include "model.hpp"
#include "output.hpp"

void RLS::Output::velocityGraphConfig()
{

  // // model config
  // // ************************************************
  // mix(gpMaker, texMaker);
  // joint(gpMaker, texMaker);
  // eeTrans(gpMaker, texMaker);
  // eeRot(gpMaker, texMaker);
  // eeWrench(gpMaker, texMaker);
  // spatialMom(gpMaker, texMaker);
  // // ************************************************

}
